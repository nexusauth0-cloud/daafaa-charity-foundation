# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# TypeScript incremental build info
*.tsbuildinfo

# Vite cache
.vite/

# Package-manager artifacts (this project installs with npm -> package-lock.json)
pnpm-lock.yaml
pnpm-workspace.yaml

# Unoptimized source images / local working copies
# (the optimized assets the site actually ships from are in /public)
Comrade Ameer.zip
/web/
/originals/
/*.jpeg